<% if record.popover.present? %>
  <a href="#" data-toggle="popover" data-content="<%= record.popover %>" data-trigger="focus">
    <span class="glyphicon glyphicon-info-sign"></span>
  </a>
<% end %>
